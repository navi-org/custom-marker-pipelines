pipeline {
   agent { label 'nodejs-app' }
   stages {
      stage('Say Hello') {
         steps {
            echo 'Hello World!'   
            sh 'java -version'
         }
      }
      stage('Test') {
        steps {
          sh 'java -version'
          container('nodejs') {
            echo 'Hello World!'              
            sh 'node --version'
          }
        }
      }
   }
}
